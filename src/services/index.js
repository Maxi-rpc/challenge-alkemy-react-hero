import { BACKEND_AUTH, BACKEND_HERO, BACKEND_HERO_SEARCH } from "./Apirest";

const Backend = {
	backend_auth: BACKEND_AUTH,
	backend_hero: BACKEND_HERO,
	backend_hero_search: BACKEND_HERO_SEARCH,
};

export default Backend;
